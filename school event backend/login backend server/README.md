[Added Backend Logic]
step run backend  locally
**step 1**
create an .env file with the necessary info inside
PORT=5000
CONNECTION_STRING=mongob uri
JWT_SECRET=my-secret-key


**step 2**
cd "school event backend"
cd "login server" 
npm run dev      
if  it does not work run npm install or copilot to debug it

url for login is "http://localhost:5000/api/auth/login"
url for  register is "http://localhost:5000/api/auth/register"

to  test watch youtube and use postman to  test them 

